% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{load_mtcars_data}
\alias{load_mtcars_data}
\title{Load Data from DuckDB Table}
\usage{
load_mtcars_data(con, table = "mtcars", default_rownames = NULL)
}
\arguments{
\item{con}{DBI connection object (active DuckDB connection).}

\item{table}{Character. Name of table to load.
Default: "mtcars"}
}
\value{
data.frame. Table data with proper column types. Returns empty
data.frame() on query failure (warns but does not abort).
}
\description{
Reads entire table from DuckDB database with type-safe retrieval.
Uses DBI::dbReadTable() for automatic type preservation.
On failure, returns empty data.frame() and warns (graceful degradation).
}
\examples{
\dontrun{
data <- load_mtcars_data(con, table = "mtcars")
}
}
\keyword{internal}
