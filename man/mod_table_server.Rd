% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mod_table.R
\name{mod_table_server}
\alias{mod_table_server}
\title{Table Module Server}
\usage{
mod_table_server(id, store_reactive, store_trigger)
}
\arguments{
\item{id}{Character string. Module namespace ID (must match UI).}

\item{store_reactive}{Reactive value containing DataStore R6 object.
Pattern: store_reactive <- reactiveVal(DataStore$new())}

\item{store_trigger}{Reactive value used to force invalidation.
Pattern: store_trigger <- reactiveVal(0)}
}
\value{
Module server function (for testServer compatibility)
}
\description{
Shiny module server implementing reactive data table with R6 state management.
Demonstrates production-grade patterns:
\itemize{
\item reactiveVal() wrapping R6 for explicit invalidation
\item Controlled reactivity with manual triggers
\item Bidirectional JS ↔ R communication
\item Type-safe cell updates through R6 validation
}
}
\section{Reactivity Pattern}{

This module uses explicit invalidation via reactiveVal():

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# Reading: triggers reactive dependency
current_store <- store_reactive()

# Writing: triggers invalidation
store_reactive()$update_cell(row, col, value)
store_trigger(store_trigger() + 1)  # Force invalidation
}\if{html}{\out{</div>}}
}

\examples{
\dontrun{
# In app_server.R:
store <- DataStore$new()
store_reactive <- reactiveVal(store)
store_trigger <- reactiveVal(0)
mod_table_server("table", store_reactive, store_trigger)
}

}
