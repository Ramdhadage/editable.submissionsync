% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hotwidget.R
\name{hotwidget}
\alias{hotwidget}
\title{Handsontable Interactive Table Widget}
\usage{
hotwidget(data, width = NULL, height = NULL, elementId = NULL)
}
\arguments{
\item{data}{Data frame to display and edit. Required.}

\item{width}{Widget width (CSS unit or NULL for auto)}

\item{height}{Widget height (CSS unit or NULL for auto)}

\item{elementId}{Explicit element ID for Shiny input binding (auto-generated if NULL)}
}
\value{
htmlwidget object
}
\description{
Custom htmlwidget wrapping Handsontable library for editable data tables.
Implements bidirectional R ↔ JS communication with single-cell edit validation.
}
\details{
This widget is designed for production Shiny applications requiring:
\itemize{
\item Deterministic single-cell edits (no batch operations)
\item Type validation in both JS and R layers
\item Controlled reactivity through explicit state management
\item Clean separation between UI (Handsontable) and state (R6)
}
}
\section{JavaScript Communication}{

The widget sends edit events to Shiny via \verb{input$<id>_edit}:

\if{html}{\out{<div class="sourceCode">}}\preformatted{\{
  row: 0,              # 0-based JavaScript index
  col: "mpg",          # Column name
  oldValue: 21.0,      # Previous value
  value: 22.5          # New value (user input)
\}
}\if{html}{\out{</div>}}
}

\examples{
\dontrun{
# Standalone usage
hotwidget(data = mtcars)

# In Shiny
output$table <- renderHotwidget({
  hotwidget(data = store$data)
})
}

}
